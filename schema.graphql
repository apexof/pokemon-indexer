# type Pokemon @entity(immutable: true) {
#   id: Bytes!
# }

type User @entity(immutable: false) {
  id: ID!
  pokemonsCount: BigInt!
  # pokemons: {
  #   total: BigInt!
  #   items: [Pokemon!]!
  # }
}